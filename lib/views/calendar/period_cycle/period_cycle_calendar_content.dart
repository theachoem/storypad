part of 'period_cycle_calendar_view.dart';

class _PeriodCycleCalendarContent extends StatelessWidget {
  const _PeriodCycleCalendarContent(this.viewModel);

  final PeriodCycleCalendarViewModel viewModel;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
