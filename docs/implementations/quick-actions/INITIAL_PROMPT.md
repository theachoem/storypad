Consider yourself a senior UX/UI designer, let's brainstorm best UX/UI quick actions feature for storypad.

We want to allow users to quickly perform common actions directly from their phone homepage without having to open the app.

This is some of the ideas we have so far:

- Default quick actions for all users:
  - new_story
  - take_photo
  - record_voice
- Quick template
  - new story with custom template/gallery template
- Specific tags
  - this will open show tag view (view all stories in that tag)

User can enable or add them limit by 5-10 base on device support. Default will be free while quick template/tag customization will be a paid feature.

We will have a quick_actions_view or a better name of its customizaton view. This will will structure as follow:

- Preview of quick actions
  - Show app icon, with quick action on top (similar to native UI)
    - For android, we can use Google San font (google_font)
    - For ios, we can ues font that similar to apple native font that provide by google font as well.
- Enabled quick actions
  - Show all quick actions in list tile with remove button
- Available quick actions to add by category
  - Default quick actions
  - Quick template
  - Specific tags

For base plan, let's focus on UX/UI only, no functionality yet. Give me 3 different design concepts for customization view of quick actions. Each concept should have a unique layout and visual style, while still maintaining a user-friendly interface.
